<svg xmlns="http://www.w3.org/2000/svg" width="1920" height="1080" viewBox="0 0 1920 1080">
  <!-- Background -->
  <rect width="1920" height="1080" fill="#0f172a"/>

  <!-- Definitions -->
  <defs>
    <!-- Radar grid pattern -->
    <pattern id="radar-grid" width="80" height="80" patternUnits="userSpaceOnUse">
      <circle cx="40" cy="40" r="40" fill="none" stroke="#1e293b" stroke-width="0.5" opacity="0.4"/>
      <circle cx="40" cy="40" r="20" fill="none" stroke="#1e293b" stroke-width="0.5" opacity="0.4"/>
      <line x1="40" y1="0" x2="40" y2="80" stroke="#1e293b" stroke-width="0.5" opacity="0.3"/>
      <line x1="0" y1="40" x2="80" y2="40" stroke="#1e293b" stroke-width="0.5" opacity="0.3"/>
    </pattern>

    <!-- Fog effect -->
    <radialGradient id="fog">
      <stop offset="0%" stop-color="#cbd5e1" stop-opacity="0.3"/>
      <stop offset="50%" stop-color="#cbd5e1" stop-opacity="0.15"/>
      <stop offset="100%" stop-color="#cbd5e1" stop-opacity="0"/>
    </radialGradient>

    <!-- Glow for collision point -->
    <filter id="collision-glow">
      <feGaussianBlur stdDeviation="8" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Radar background -->
  <rect width="1920" height="1080" fill="url(#radar-grid)"/>

  <!-- Runway (top-down view) -->
  <g opacity="0.6">
    <!-- Main runway -->
    <rect x="760" y="200" width="400" height="700" fill="#1e293b" stroke="#475569" stroke-width="3"/>

    <!-- Center line -->
    <line x1="960" y1="200" x2="960" y2="900" stroke="#fbbf24" stroke-width="4" stroke-dasharray="40,30" opacity="0.8"/>

    <!-- Runway edge markings -->
    <line x1="770" y1="200" x2="770" y2="900" stroke="#fbbf24" stroke-width="2"/>
    <line x1="1150" y1="200" x2="1150" y2="900" stroke="#fbbf24" stroke-width="2"/>

    <!-- Runway numbers -->
    <text x="960" y="280" font-family="monospace" font-size="72" fill="#fbbf24" text-anchor="middle" font-weight="bold" opacity="0.6">12</text>
    <text x="960" y="870" font-family="monospace" font-size="72" fill="#fbbf24" text-anchor="middle" font-weight="bold" opacity="0.6" transform="rotate(180 960 850)">30</text>
  </g>

  <!-- Boeing 747 #1 (KLM) - Top-down view, on runway -->
  <g transform="translate(960, 520) rotate(0)" stroke="#00d4aa" stroke-width="3" fill="none" stroke-linecap="round" stroke-linejoin="round">
    <!-- Fuselage -->
    <ellipse cx="0" cy="0" rx="35" ry="120"/>

    <!-- Cockpit -->
    <path d="M -25,-110 Q 0,-125 25,-110" fill="#00d4aa" opacity="0.3"/>

    <!-- Wings -->
    <ellipse cx="0" cy="-10" rx="150" ry="40"/>

    <!-- Wing details -->
    <line x1="-150" y1="-10" x2="-35" y2="-10" stroke-width="2"/>
    <line x1="150" y1="-10" x2="35" y2="-10" stroke-width="2"/>

    <!-- Engines -->
    <ellipse cx="-110" cy="-10" rx="12" ry="22" fill="#00d4aa" opacity="0.4"/>
    <ellipse cx="-70" cy="-10" rx="12" ry="22" fill="#00d4aa" opacity="0.4"/>
    <ellipse cx="70" cy="-10" rx="12" ry="22" fill="#00d4aa" opacity="0.4"/>
    <ellipse cx="110" cy="-10" rx="12" ry="22" fill="#00d4aa" opacity="0.4"/>

    <!-- TAIL INDICATOR (vertical stabilizer) -->
    <path d="M -15,100 L 0,125 L 15,100" fill="#00d4aa" opacity="0.6"/>
    <rect x="-8" y="100" width="16" height="30" fill="#00d4aa" opacity="0.5"/>

    <!-- Aircraft label -->
    <text x="0" y="170" font-family="monospace" font-size="18" fill="#00d4aa" text-anchor="middle" font-weight="bold">KL4805</text>
  </g>

  <!-- Boeing 747 #2 (Pan Am) - Top-down view, entering runway -->
  <g transform="translate(860, 620) rotate(-30)" stroke="#38bdf8" stroke-width="3" fill="none" stroke-linecap="round" stroke-linejoin="round">
    <!-- Fuselage -->
    <ellipse cx="0" cy="0" rx="35" ry="120"/>

    <!-- Cockpit -->
    <path d="M -25,-110 Q 0,-125 25,-110" fill="#38bdf8" opacity="0.3"/>

    <!-- Wings -->
    <ellipse cx="0" cy="-10" rx="150" ry="40"/>

    <!-- Wing details -->
    <line x1="-150" y1="-10" x2="-35" y2="-10" stroke-width="2"/>
    <line x1="150" y1="-10" x2="35" y2="-10" stroke-width="2"/>

    <!-- Engines -->
    <ellipse cx="-110" cy="-10" rx="12" ry="22" fill="#38bdf8" opacity="0.4"/>
    <ellipse cx="-70" cy="-10" rx="12" ry="22" fill="#38bdf8" opacity="0.4"/>
    <ellipse cx="70" cy="-10" rx="12" ry="22" fill="#38bdf8" opacity="0.4"/>
    <ellipse cx="110" cy="-10" rx="12" ry="22" fill="#38bdf8" opacity="0.4"/>

    <!-- TAIL INDICATOR (vertical stabilizer) -->
    <path d="M -15,100 L 0,125 L 15,100" fill="#38bdf8" opacity="0.6"/>
    <rect x="-8" y="100" width="16" height="30" fill="#38bdf8" opacity="0.5"/>

    <!-- Aircraft label -->
    <text x="0" y="170" font-family="monospace" font-size="18" fill="#38bdf8" text-anchor="middle" font-weight="bold">PA1736</text>
  </g>

  <!-- Collision point indicator -->
  <g filter="url(#collision-glow)">
    <circle cx="920" cy="560" r="60" fill="none" stroke="#ff4757" stroke-width="4" opacity="0.6"/>
    <circle cx="920" cy="560" r="80" fill="none" stroke="#ff4757" stroke-width="3" opacity="0.4"/>
    <circle cx="920" cy="560" r="100" fill="none" stroke="#ff4757" stroke-width="2" opacity="0.2"/>

    <!-- Collision flash -->
    <circle cx="920" cy="560" r="20" fill="#ff4757" opacity="0.5"/>
  </g>

  <!-- Flight paths (showing movement) -->
  <g stroke-width="2" stroke-dasharray="10,5" opacity="0.5">
    <!-- KLM takeoff path -->
    <path d="M 960,900 L 960,520" stroke="#00d4aa" fill="none"/>
    <polygon points="960,520 955,535 965,535" fill="#00d4aa"/>

    <!-- Pan Am taxi path -->
    <path d="M 600,720 L 860,620" stroke="#38bdf8" fill="none"/>
    <polygon points="860,620 850,625 855,635" fill="#38bdf8" transform="rotate(-30 860 620)"/>
  </g>

  <!-- Fog overlay -->
  <circle cx="960" cy="540" r="400" fill="url(#fog)"/>
  <circle cx="700" cy="400" r="350" fill="url(#fog)"/>
  <circle cx="1200" cy="650" r="380" fill="url(#fog)"/>

  <!-- Top identifier -->
  <g font-family="monospace" text-anchor="middle">
    <text x="960" y="120" font-size="56" fill="#fbbf24" font-weight="bold">KL4805 &amp; PA1736</text>
    <text x="960" y="165" font-size="22" fill="#94a3b8">27 MARCH 1977</text>
    <text x="960" y="195" font-size="22" fill="#94a3b8">LOS RODEOS AIRPORT, SPAIN</text>
  </g>

  <!-- Collision callout -->
  <g font-family="monospace" fill="#ff4757">
    <line x1="920" y1="660" x2="1100" y2="780" stroke="#ff4757" stroke-width="2.5"/>
    <text x="1110" y="785" font-size="20" font-weight="bold">RUNWAY COLLISION</text>
    <text x="1110" y="810" font-size="16" fill="#94a3b8">Low visibility fog</text>
  </g>

  <!-- Bottom info -->
  <g>
    <rect x="0" y="1000" width="1920" height="80" fill="#1e293b" opacity="0.8"/>
    <text x="100" y="1050" font-family="monospace" font-size="32" fill="#ff4757" font-weight="bold">583 FATALITIES</text>
    <text x="1820" y="1050" font-family="monospace" font-size="20" fill="#64748b" text-anchor="end">BOEING 747 (×2)</text>
  </g>
</svg>
